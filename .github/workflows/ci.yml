name: CI

on:
  push:
    branches: ["**"]
  pull_request:

env:
  HOMEBREW_NO_AUTO_UPDATE: "1"

jobs:
  # Rask Linux-jobb uten Mac: bygger kjernen (motor, MesterAI, Elo og
  # companion-poengføring) med SwiftPM, kjører alle enhetstestene og
  # legger en MesterAI-demokamp + companion-demo i jobbsammendraget –
  # slik at AI-en kan ses «i aksjon» rett i nettleseren.
  linux-kjerne:
    name: Linux – kjernetester og MesterAI i aksjon
    runs-on: ubuntu-latest
    container: swift:6.0-noble
    defaults:
      run:
        # Containerens standard-shell er dash, som mangler `set -o pipefail`.
        shell: bash
    steps:
      - uses: actions/checkout@v5

      - name: Kjør kjernetestene (SwiftPM)
        run: |
          set -o pipefail
          swift test 2>&1 | tee kjernetest.log | tail -20

      - name: Bygg kommandolinjeverktøyet (release)
        run: swift build -c release

      - name: "MesterAI i aksjon: demoparti"
        run: |
          set -o pipefail
          .build/release/Amerikaneren demo --seed 42 --runder 4 --tid 0.05 | tee demo.log
          {
            echo "## 🃏 MesterAI i aksjon (seed 42, 4 runder, sete 0 = President)"
            echo '```'
            cat demo.log
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: "MesterAI i aksjon: miniarena"
        run: |
          set -o pipefail
          .build/release/Amerikaneren arena --partier 3 --mot vanskelig --seed 7 --runder 8 --tid 0.05 | tee arena.log
          {
            echo "## 🏟️ Miniarena – MesterAI mot tre «Vanskelig» (3 partier à 8 runder)"
            echo '```'
            cat arena.log
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: "Hot-seat-røyktest (menyene godtar alltid «1»)"
        run: |
          set -o pipefail
          { printf '2\n'; yes 1; } | .build/release/Amerikaneren spill \
            --seter menneske,president,menneske,president \
            --seed 11 --runder 1 --tid 0.05 | tail -8

      - name: "Companion-poengføring: demo"
        run: |
          set -o pipefail
          .build/release/Amerikaneren companion --demo | tee companion.log
          {
            echo "## 📝 Companion-poengføring i aksjon"
            echo '```'
            cat companion.log
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Feillogg til sammendraget
        if: failure()
        run: |
          {
            echo "### ❌ Linux-jobben feilet – siste logglinjer"
            echo '```'
            for f in kjernetest.log demo.log arena.log companion.log; do
              if [ -f "$f" ]; then
                echo "== $f =="
                tail -60 "$f"
              fi
            done
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

  build-and-test:
    name: Bygg, test og lag simulatorbygg
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v5

      # Nyeste XcodeGen genererer prosjektformat 77, som krever Xcode 16.3+.
      # Velg nyeste installerte Xcode eksplisitt så gamle standardversjoner
      # på runneren ikke feiler med "future Xcode project file format".
      - name: Velg nyeste Xcode
        run: |
          NYESTE=$(ls -d /Applications/Xcode_*.app | sort -V | tail -1)
          echo "Velger $NYESTE"
          sudo xcode-select -s "$NYESTE"
          xcodebuild -version

      - name: Installer XcodeGen
        run: brew install xcodegen

      - name: Generer Xcode-prosjekt
        run: |
          xcodegen generate
          xcodebuild -list -project Amerikaneren.xcodeproj

      - name: Vis byggemiljø og velg simulator
        id: sim
        run: |
          xcodebuild -version
          xcrun simctl list devices available | sed -n '1,30p'
          # Velg første tilgjengelige iPhone-simulator dynamisk, i stedet
          # for å hardkode et modellnavn som kan mangle på runneren.
          DEVICE=$(xcrun simctl list devices available \
            | grep -E "iPhone" | head -1 \
            | sed -E 's/^ *(.+) \([0-9A-F-]+\) \(.*/\1/')
          if [ -z "$DEVICE" ]; then
            echo "Fant ingen iPhone-simulator!" >&2
            exit 1
          fi
          echo "Valgt simulator: $DEVICE"
          echo "device=$DEVICE" >> "$GITHUB_OUTPUT"

      - name: Bygg og kjør tester
        run: |
          set -o pipefail
          xcodebuild test \
            -project Amerikaneren.xcodeproj \
            -scheme Amerikaneren \
            -destination "platform=iOS Simulator,name=${{ steps.sim.outputs.device }}" \
            CODE_SIGNING_ALLOWED=NO \
            2>&1 | tee test.log | tail -60

      - name: Bygg simulator-app (Release)
        run: |
          set -o pipefail
          xcodebuild build \
            -project Amerikaneren.xcodeproj \
            -scheme Amerikaneren \
            -configuration Release \
            -destination 'generic/platform=iOS Simulator' \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            2>&1 | tee release.log | tail -30

      - name: Pakk simulatorbygget
        run: |
          cd build/Build/Products/Release-iphonesimulator
          zip -r "$GITHUB_WORKSPACE/Amerikaneren-simulator.zip" Amerikaneren.app

      - name: Last opp simulatorbygg som artifact
        uses: actions/upload-artifact@v4
        with:
          name: Amerikaneren-simulator
          path: Amerikaneren-simulator.zip
          retention-days: 14

      # Automatisk opplasting til Appetize hvis APPETIZE_API_TOKEN-secret
      # er satt i repoet. Sett også APPETIZE_PUBLIC_KEY for å oppdatere
      # samme app-lenke hver gang i stedet for å lage en ny.
      - name: Last opp til Appetize (valgfritt)
        env:
          APPETIZE_API_TOKEN: ${{ secrets.APPETIZE_API_TOKEN }}
          APPETIZE_PUBLIC_KEY: ${{ secrets.APPETIZE_PUBLIC_KEY }}
        run: |
          if [ -z "$APPETIZE_API_TOKEN" ]; then
            echo "APPETIZE_API_TOKEN ikke satt – hopper over opplasting."
            echo "Last ned artifactet og last opp manuelt på https://appetize.io/upload"
            exit 0
          fi
          # v1-endepunktet tar direkte filopplasting (multipart);
          # v2 aksepterer kun JSON med en offentlig URL.
          if [ -n "$APPETIZE_PUBLIC_KEY" ]; then
            URL="https://api.appetize.io/v1/apps/$APPETIZE_PUBLIC_KEY"
          else
            URL="https://api.appetize.io/v1/apps"
          fi
          # Robust opplasting: retries og HTTP/1.1 (multipart over store
          # filer kan gi tilkoblingsfeil), og et mislykket forsøk skal
          # ikke velte et ellers grønt bygg – artifactet finnes uansett.
          HTTP=$(curl -sS --http1.1 --retry 3 --retry-delay 3 --retry-all-errors \
            -o appetize-svar.json -w "%{http_code}" \
            -u "$APPETIZE_API_TOKEN:" \
            -F "file=@Amerikaneren-simulator.zip" \
            -F "platform=ios" \
            "$URL") || HTTP="000"
          echo "HTTP-status: $HTTP"
          # Ikke skriv hele svaret til loggen – det inneholder privateKey.
          NOKKEL=$(python3 -c 'import json; print(json.load(open("appetize-svar.json")).get("publicKey",""))' 2>/dev/null || true)
          echo "publicKey: ${NOKKEL:-<mangler – HTTP $HTTP>}"
          if [ -n "$NOKKEL" ]; then
            echo "### 🎮 Spill appen i nettleseren" >> "$GITHUB_STEP_SUMMARY"
            echo "https://appetize.io/app/$NOKKEL" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "(Sett secret APPETIZE_PUBLIC_KEY til \`$NOKKEL\` for å beholde samme lenke.)" >> "$GITHUB_STEP_SUMMARY"
          else
            echo "### ⚠️ Appetize-opplastingen feilet (HTTP $HTTP)" >> "$GITHUB_STEP_SUMMARY"
            echo "Simulatorbygget ligger som artifact – last det opp manuelt på https://appetize.io/upload" >> "$GITHUB_STEP_SUMMARY"
          fi

      # Ved feil: legg de siste logglinjene rett i jobbsammendraget, så
      # feilen kan leses (og limes inn) uten å grave i rålogger.
      - name: Feillogg til sammendraget
        if: failure()
        run: |
          {
            echo "### ❌ Bygget feilet – siste logglinjer"
            echo '```'
            for f in test.log release.log; do
              if [ -f "$f" ]; then
                echo "== $f =="
                grep -E "error:|fatal|failed|Testing failed|BUILD FAILED" "$f" | head -40 || true
                tail -80 "$f"
              fi
            done
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Last opp fulle logger ved feil
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: byggelogger
          path: |
            test.log
            release.log
          if-no-files-found: ignore
          retention-days: 7
